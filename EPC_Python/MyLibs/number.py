from region import Region

class Number:
	def __init__(self, npoints, universe_size, region_min, reion_max):
		self.number = Region(name='number_'+str(number), npoints=npoints, universe_size=universe_size, region_min=region_min, region_min=region_min, 'triangular')